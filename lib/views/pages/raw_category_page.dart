import 'package:flutter/material.dart';

class RawCategoryPage extends StatelessWidget {
  const RawCategoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Raw Category Page'),
      ),
    );
  }
}

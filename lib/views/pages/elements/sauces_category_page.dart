import 'package:flutter/material.dart';

class SaucesCategoryPage extends StatelessWidget {
  const SaucesCategoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sauces Category Page'),
      ),
    );
  }
}
